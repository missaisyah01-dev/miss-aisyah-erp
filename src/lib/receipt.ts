export type ReceiptItem = { product_name: string; variant_name?: string | null; quantity: number; unit_price: number; subtotal: number };
export type Receipt = { invoice_number: string; created_at: string; payment_method: string; total: number; paid_amount: number; change_amount: number; subtotal?: number; discount_amount?: number; customer_name?: string | null; notes?: string | null; items: ReceiptItem[] };
function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character] ?? character); }
export function printReceipt(receipt: Receipt) { const rows = receipt.items.map((item) => `<tr><td>${escapeHtml(item.product_name)}${item.variant_name ? `<br><small>${escapeHtml(item.variant_name)}</small>` : ""}</td><td>${item.quantity} x ${Number(item.unit_price).toLocaleString("id-ID")}</td><td style="text-align:right">${Number(item.subtotal).toLocaleString("id-ID")}</td></tr>`).join(""); const remaining = Math.max(0, Number(receipt.total) - Number(receipt.paid_amount)); const discount = Number(receipt.discount_amount ?? 0); const subtotal = Number(receipt.subtotal ?? receipt.total + discount); const page = window.open("", "_blank", "width=420,height=700"); if (!page) return alert("Izinkan pop-up untuk mencetak struk."); page.document.write(`<!doctype html><html><head><title>${escapeHtml(receipt.invoice_number)}</title><style>body{font:13px Arial;padding:22px;color:#222}h1{text-align:center;font-size:19px;margin:0}p{text-align:center;color:#666;margin:6px}table{width:100%;border-collapse:collapse;margin-top:18px}td{padding:7px 0;border-bottom:1px solid #eee;vertical-align:top}small{color:#666}.total{font-size:16px;font-weight:bold}.right{text-align:right}.alert{color:#92400e;font-weight:bold}</style></head><body><h1>MISS AISYAH</h1><p>Struk Penjualan</p><p>${escapeHtml(receipt.invoice_number)}<br>${new Date(receipt.created_at).toLocaleString("id-ID")}${receipt.customer_name ? `<br>Pelanggan: ${escapeHtml(receipt.customer_name)}` : ""}</p><table>${rows}</table><table><tr><td>Subtotal</td><td class="right">Rp ${subtotal.toLocaleString("id-ID")}</td></tr>${discount ? `<tr><td>Diskon</td><td class="right">- Rp ${discount.toLocaleString("id-ID")}</td></tr>` : ""}<tr><td>Total</td><td class="right total">Rp ${Number(receipt.total).toLocaleString("id-ID")}</td></tr><tr><td>Dibayar (${escapeHtml(receipt.payment_method)})</td><td class="right">Rp ${Number(receipt.paid_amount).toLocaleString("id-ID")}</td></tr>${remaining ? `<tr class="alert"><td>Sisa piutang</td><td class="right">Rp ${remaining.toLocaleString("id-ID")}</td></tr>` : `<tr><td>Kembalian</td><td class="right">Rp ${Number(receipt.change_amount).toLocaleString("id-ID")}</td></tr>`}</table>${receipt.notes ? `<p>${escapeHtml(receipt.notes)}</p>` : ""}<p>Terima kasih telah berbelanja.</p><script>window.onload=()=>window.print()</script></body></html>`); page.document.close(); }
