"use client";

import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";

type Item = { id: number; product_name: string; variant_name: string | null; quantity: number; unit_price: number; transactions: { invoice_number: string; created_at: string } | null };
const rupiah = (value: number) => new Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", maximumFractionDigits: 0 }).format(value);

export default function ReturnsPanel() {
  const [items, setItems] = useState<Item[]>([]); const [selected, setSelected] = useState<Item | null>(null); const [quantity, setQuantity] = useState("1"); const [method, setMethod] = useState("CASH"); const [reason, setReason] = useState(""); const [saving, setSaving] = useState(false);
  async function load() { const { data, error } = await supabase.from("transaction_items").select("id,product_name,variant_name,quantity,unit_price,transactions!inner(invoice_number,created_at)").order("created_at", { ascending: false }).limit(20); if (error) alert(`Gagal memuat item retur: ${error.message}`); else setItems((data ?? []) as Item[]); }
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { void load(); }, []);
  async function submit() { if (!selected) return; const count = Number(quantity); if (!Number.isInteger(count) || count < 1 || count > Number(selected.quantity)) return alert("Jumlah retur tidak valid."); setSaving(true); const { error } = await supabase.rpc("return_transaction_item", { p_transaction_item_id: selected.id, p_quantity: count, p_refund_method: method, p_reason: reason || null }); setSaving(false); if (error) return alert(`Retur gagal: ${error.message}`); alert(`Retur ${rupiah(Number(selected.unit_price) * count)} berhasil dicatat dan stok dikembalikan.`); setSelected(null); setReason(""); void load(); }
  return <section className="mt-8 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="text-xl font-bold text-gray-900">Retur barang</h2><p className="mt-1 text-sm text-gray-500">Pilih item yang dikembalikan. Stok varian akan bertambah otomatis.</p></div><span className="rounded-full bg-violet-100 px-3 py-1 text-xs font-bold text-violet-700">Audit tersimpan</span></div><div className="mt-5 overflow-x-auto rounded-xl border border-gray-200"><table className="w-full min-w-[700px] text-sm"><thead className="bg-gray-50 text-left text-xs uppercase tracking-wide text-gray-500"><tr><th className="px-4 py-3">Invoice</th><th className="px-4 py-3">Produk</th><th className="px-4 py-3 text-right">Harga</th><th className="px-4 py-3 text-center">Aksi</th></tr></thead><tbody>{items.length ? items.map((item) => <tr key={item.id} className="border-t border-gray-100"><td className="px-4 py-3"><b>{item.transactions?.invoice_number ?? "-"}</b><small className="block text-gray-500">{item.transactions ? new Date(item.transactions.created_at).toLocaleDateString("id-ID") : ""}</small></td><td className="px-4 py-3"><b>{item.product_name}</b>{item.variant_name && <small className="block text-pink-700">{item.variant_name}</small>}<small className="block text-gray-500">Terjual {item.quantity} pcs</small></td><td className="px-4 py-3 text-right font-semibold">{rupiah(Number(item.unit_price))}</td><td className="px-4 py-3 text-center"><button onClick={() => { setSelected(item); setQuantity("1"); setMethod("CASH"); setReason(""); }} className="rounded-lg bg-violet-100 px-3 py-1.5 text-xs font-semibold text-violet-700">Retur</button></td></tr>) : <tr><td colSpan={4} className="px-4 py-10 text-center text-gray-500">Belum ada item transaksi.</td></tr>}</tbody></table></div>{selected && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-5 shadow-xl"><div className="flex justify-between gap-4"><div><h3 className="text-lg font-bold">Catat retur</h3><p className="mt-1 text-sm text-gray-600">{selected.product_name} {selected.variant_name ? `(${selected.variant_name})` : ""}</p></div><button onClick={() => setSelected(null)} className="rounded-lg bg-gray-100 px-3 py-2 text-sm">Tutup</button></div><label className="mt-4 block text-sm font-medium text-gray-700">Jumlah (maks. {selected.quantity})<input value={quantity} onChange={(event) => setQuantity(event.target.value)} type="number" min="1" max={selected.quantity} className="mt-1 w-full rounded-xl border border-gray-300 px-3 py-2.5" /></label><label className="mt-3 block text-sm font-medium text-gray-700">Metode pengembalian dana<select value={method} onChange={(event) => setMethod(event.target.value)} className="mt-1 w-full rounded-xl border border-gray-300 px-3 py-2.5"><option value="CASH">Tunai</option><option value="QRIS">QRIS</option><option value="DEBIT">Debit</option></select></label><label className="mt-3 block text-sm font-medium text-gray-700">Alasan retur<textarea value={reason} onChange={(event) => setReason(event.target.value)} placeholder="Contoh: ukuran tidak sesuai" className="mt-1 min-h-20 w-full rounded-xl border border-gray-300 px-3 py-2.5" /></label><p className="mt-3 rounded-xl bg-violet-50 p-3 text-sm text-violet-900">Dana tercatat: <b>{rupiah(Number(selected.unit_price) * (Number(quantity) || 0))}</b></p><button disabled={saving} onClick={() => void submit()} className="mt-4 w-full rounded-xl bg-violet-600 px-4 py-3 font-semibold text-white disabled:bg-violet-300">{saving ? "Menyimpan..." : "Konfirmasi retur"}</button></div></div>}</section>;
}
