type Activity = { product: string; tipe: string; jumlah: number; created_at: string }; type Props = { activities: Activity[] };
export default function RecentActivity({ activities }: Props) { return <section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div><h2 className="text-lg font-bold text-gray-900">Aktivitas terbaru</h2><p className="mt-1 text-sm text-gray-500">Pergerakan stok terakhir yang tercatat.</p></div>{activities.length === 0 ? <p className="py-10 text-center text-sm text-gray-500">Belum ada aktivitas.</p> : <div className="mt-5 divide-y divide-gray-100">{activities.map((item, index) => <div key={`${item.product}-${index}`} className="flex items-center justify-between gap-3 py-3 first:pt-0 last:pb-0"><div className="min-w-0"><p className="truncate font-semibold text-gray-900">{item.product}</p><p className="mt-0.5 text-sm text-gray-500">{item.tipe === "MASUK" ? "Stok masuk" : item.tipe === "KELUAR" ? "Stok keluar" : "Retur"} · {item.jumlah} pcs</p></div><time className="shrink-0 text-xs text-gray-400">{new Date(item.created_at).toLocaleDateString("id-ID", { day: "numeric", month: "short" })}</time></div>)}</div>}</section>; }
