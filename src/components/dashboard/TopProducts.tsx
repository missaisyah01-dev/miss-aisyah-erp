type Product = { product_name: string; total_quantity: number; total_revenue: number }; type Props = { products: Product[] };
const rupiah = (amount: number) => new Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", maximumFractionDigits: 0 }).format(amount);
export default function TopProducts({ products }: Props) { return <section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div><h2 className="text-lg font-bold text-gray-900">Produk terlaris</h2><p className="mt-1 text-sm text-gray-500">Lima produk dengan jumlah penjualan tertinggi.</p></div>{products.length === 0 ? <p className="py-10 text-center text-sm text-gray-500">Belum ada produk terjual.</p> : <ol className="mt-5 space-y-2">{products.map((item, index) => <li key={`${item.product_name}-${index}`} className="flex items-center gap-3 rounded-xl bg-gray-50 px-3 py-3"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-pink-100 text-xs font-bold text-pink-700">{index + 1}</span><div className="min-w-0 flex-1"><p className="truncate font-semibold text-gray-900">{item.product_name}</p><p className="mt-0.5 text-xs text-gray-500">{rupiah(Number(item.total_revenue))}</p></div><span className="rounded-lg bg-white px-2.5 py-1 text-sm font-bold text-pink-700 shadow-sm ring-1 ring-gray-200">{item.total_quantity} pcs</span></li>)}</ol>}</section>; }
