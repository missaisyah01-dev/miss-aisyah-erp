type Product = { nama: string; stok: number }; type Props = { products: Product[] };
export default function TopProducts({ products }: Props) { return <section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div><h2 className="text-lg font-bold text-gray-900">Produk dengan stok terbanyak</h2><p className="mt-1 text-sm text-gray-500">Lima produk dengan ketersediaan tertinggi.</p></div>{products.length === 0 ? <p className="py-10 text-center text-sm text-gray-500">Belum ada produk.</p> : <ol className="mt-5 space-y-2">{products.map((item, index) => <li key={`${item.nama}-${index}`} className="flex items-center gap-3 rounded-xl bg-gray-50 px-3 py-3"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-pink-100 text-xs font-bold text-pink-700">{index + 1}</span><p className="min-w-0 flex-1 truncate font-semibold text-gray-900">{item.nama}</p><span className="rounded-lg bg-white px-2.5 py-1 text-sm font-bold text-pink-700 shadow-sm ring-1 ring-gray-200">{item.stok}</span></li>)}</ol>}</section>; }
