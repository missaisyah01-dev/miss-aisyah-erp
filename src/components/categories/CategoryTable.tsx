type Category = { id: number; nama: string }; type Props = { categories: Category[]; onEdit: (category: Category) => void; onDelete: (id: number) => void };
export default function CategoryTable({ categories, onEdit, onDelete }: Props) { return <div className="overflow-x-auto rounded-xl border border-gray-200 bg-white shadow-sm"><table className="w-full min-w-[500px] text-sm"><thead className="bg-pink-600 text-left text-xs uppercase tracking-wide text-white"><tr><th className="p-4">Nama kategori</th><th className="p-4 text-center">Aksi</th></tr></thead><tbody>{categories.length === 0 ? <tr><td colSpan={2} className="p-10 text-center text-gray-500">Belum ada kategori.</td></tr> : categories.map((category) => <tr key={category.id} className="border-b border-gray-100 hover:bg-pink-50"><td className="p-4 font-semibold text-gray-900">{category.nama}</td><td className="p-4"><div className="flex justify-center gap-2"><button onClick={() => onEdit(category)} className="rounded-lg bg-pink-100 px-3 py-1.5 font-semibold text-pink-700 hover:bg-pink-200">Edit</button><button onClick={() => onDelete(category.id)} className="rounded-lg bg-red-100 px-3 py-1.5 font-semibold text-red-700 hover:bg-red-200">Hapus</button></div></td></tr>)}</tbody></table></div>; }
