type Product = { id: number; kode: string; nama: string; kategori: string; harga: number; stok: number };
type Props = { products: Product[]; onEdit: (product: Product) => void; onDelete: (id: number) => void };
export default function ProductTable({ products, onEdit, onDelete }: Props) { return <div className="overflow-x-auto rounded-xl border border-gray-200 bg-white shadow-sm"><table className="w-full min-w-[760px] text-sm"><thead className="bg-pink-600 text-left text-xs uppercase tracking-wide text-white"><tr><th className="p-4">SKU</th><th className="p-4">Nama produk</th><th className="p-4">Kategori</th><th className="p-4">Stok</th><th className="p-4">Harga</th><th className="p-4 text-center">Aksi</th></tr></thead><tbody>{products.length === 0 ? <tr><td colSpan={6} className="p-10 text-center text-gray-500">Belum ada produk.</td></tr> : products.map((product) => <tr key={product.id} className="border-b border-gray-100 hover:bg-pink-50"><td className="p-4 font-medium text-gray-700">{product.kode}</td><td className="p-4 font-semibold text-gray-900">{product.nama}</td><td className="p-4"><span className="rounded-full bg-pink-100 px-3 py-1 text-xs font-medium text-pink-700">{product.kategori}</span></td><td className="p-4 text-gray-700">{product.stok}</td><td className="p-4 font-semibold text-gray-900">Rp {product.harga.toLocaleString("id-ID")}</td><td className="p-4"><div className="flex justify-center gap-2"><button onClick={() => onEdit(product)} className="rounded-lg bg-pink-100 px-3 py-1.5 font-semibold text-pink-700 hover:bg-pink-200">Edit</button><button onClick={() => onDelete(product.id)} className="rounded-lg bg-red-100 px-3 py-1.5 font-semibold text-red-700 hover:bg-red-200">Hapus</button></div></td></tr>)}</tbody></table></div>; }
